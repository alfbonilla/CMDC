<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="rids">RID Close Out Values Index</h2>

<% if @cm_rid_close_outs.empty? %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% else %>
  <%= render :partial => 'cm_rid_close_outs/list' %>
<% end %>

<%= link_to "Add new RID close out value",
  {:action => 'new', :id => @project}, :class => 'icon icon-add' %>

<% html_title "CM - RID Close Out Values List" %>
