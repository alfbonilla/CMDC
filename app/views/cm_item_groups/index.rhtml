<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="iitems">Item Group Index</h2>

<% if @cm_item_groups.empty? %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% else %>
  <%= render :partial => 'cm_item_groups/list' %>
<% end %>

<%= link_to "Add new item group", {:controller => "cm_item_groups",
  :action => 'new', :id => @project}, :class => 'icon icon-add' %>

<% html_title "CM - Groups List" %>