<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="ncrs">Non-Conformances Phases Index</h2>

<% if @cm_ncs_phases.empty? %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% else %>
  <%= render :partial => 'cm_ncs_phases/list' %>
<% end %>

<%= link_to "Add new Non-Conformance Phase", {:controller => "cm_ncs_Phases",
  :action => 'new', :id => @project}, :class => 'icon icon-add' %>

<% html_title "CM - NC Phases List" %>