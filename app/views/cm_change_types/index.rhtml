<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="changes">Change Types Index</h2>

<% if @cm_change_types.empty? %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% else %>
  <%= render :partial => 'cm_change_types/list' %>
<% end %>

<%= link_to "Add new change type",
  {:action => 'new', :id => @project}, :class => 'icon icon-add' %>

<% html_title "CM - Change Types List" %>
