<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="reqs">Traceability Elements Tree Index</h2>

<div id="table" class="autoscroll">
  <% if @cm_info_reqs.empty? %>
    <p class="nodata"><%= l(:label_no_data) %></p>
  <% else %>
    <%= render :partial => 'cm_reqs/tree_list' %>
  <% end %>
</div>

<% html_title "CM - TEs Tree List" %>
