<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="boards">Meeting Types Index</h2>

<title>Index of Meeting Types</title>

<% if @cm_board_types.empty? %>
<p class="nodata"><%= l(:label_no_data) %></p>
<% else %>
<%= render :partial => 'cm_board_types/list' %>
<% end %>

<%= link_to "Add new meeting type", {:controller => "cm_board_types", :action => 'new', :id => @project}, :class => 'icon icon-add' %>

<% html_title "CM - Meeting Types List" %>
