<% labelled_tabular_form_for :cm_delivery, @cm_delivery,
                             :url => {:action => 'edit', :id => @cm_delivery},
                             :html => {:id => 'cm_delivery-form',
                                       :class => nil,
                                       :multipart => true} do |f| %>

  <%= error_messages_for 'cm_delivery' %>

  <div class="box">
    <fieldset class="tabular"><legend>Delivery Properties</legend>
      <div id="attributes" class="attributes">
        <%= render :partial => 'attributes', :locals => {:f => f} %>
      </div>
    </fieldset>

    <fieldset><legend>Notes</legend>
      <%= text_area_tag 'notes', @notes, :cols => 60, :rows => 10, :class => 'wiki-edit' %>
      <%= wikitoolbar_for 'notes' %>
      <%= call_hook(:view_cm_deliveries_edit_notes_bottom, { :cm_delivery => @cm_delivery, :notes => @notes, :form => f }) %>
    </fieldset>

    <%= submit_tag l(:button_submit) %>
  </div>

<% end %>
