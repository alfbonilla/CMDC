<%= stylesheet_link_tag 'cmStyle', :plugin => 'redmine_cm' %>

<h2 class="iitems">Item Types Index</h2>

<% if @cm_item_types.empty? %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% else %>
  <%= render :partial => 'cm_item_types/list' %>
<% end %>

<%= link_to "Add new item type",
  {:action => 'new', :id => @project}, :class => 'icon icon-add' %>

<% html_title "CM - Item Types List" %>
